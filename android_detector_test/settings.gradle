pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ForgetmenotDetectorTest"
include ':app'
include ':forgetmenot-mediapipe'
project(':forgetmenot-mediapipe').projectDir = file(
    '../Assets/Plugins/Android/ForgetmenotMediaPipe.androidlib'
)
