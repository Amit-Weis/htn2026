Run `python forgetmenot_cv/training/deploy_model.py` from the repository root to
place the latest exported `hacker_card.onnx` model in this directory.

From the repository root, run:

```bash
python3 forgetmenot_cv/download_model.py --android
```
